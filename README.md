# 2026 UCAS 汇编语言 第一次大作业
- 我们使用x86-32，AT&T语法
- 尝试使用多文件结构，便于维护（？）
- 程序要求：
  - 从文本文件读入文本（包含英文单词，空格和标点）
  - 处理文本数据（分割单词，统计单词）
  - 找出出现次数最多的单词
# Todo List
- [x] C/C++实现，确定算法
  - [x] 暴力逐字符匹配，不用哈希了
- [ ] 程序主体框架
  - [ ] 程序入口（主函数调用）
  - [x] 程序退出`as_exit`函数
- [ ] 文本读入`file_input`函数
- [ ] 单词分割`split`函数
- [ ] 单词保存、统计`word_counter`函数
- [ ] 取最大值`max_cnt`函数
- [ ] 输出`output`函数（只输出出现最多的单词和次数）
- [x] 附文件说明分组情况（该文件加进.gitignore）
# 编译方法
- makefile已写好，linux环境下cd到项目根目录`make`即可
- 可运行文件我设置在`/bin`中，在项目根目录下`./bin/wor_counter`即可运行
# 其他
- 正确使用git，不要把其他人的劳动成果给搞没了
- 只修改/src中的文件，其他东西不要乱改